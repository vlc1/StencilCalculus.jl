var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/#Guide","page":"Guide","text":"An end-to-end tour: from a symbolic grid expression to an assembled sparse matrix.","title":"Guide"},{"category":"section","location":"guide/#Building-expressions","page":"Guide","text":"Discrete fields are Slots; cell-level parameters are Symbolics (named, runtime-substituted) and Consts (literal); both come from StencilCore. Pointwise operators combine them, and the non-local functors δ₊/δ₋/σ₊/σ₋ apply forward/backward differences and sums along a chosen axis. Indexing a slot shifts it by a lattice offset:\n\nusing StencilCalculus\n\n@slot f Float64\ng = f[-2ê₁] - 4f[-ê₁] + 3f[]      # f[i-2] - 4 f[i-1] + 3 f[i]\n@slot ψ Float64\nadv = ψ * δ₊{1}(f)                # ψ[i] * (f[i+1] - f[i])\n\nThe @slot / @symbolic / @const macros bind a variable to a leaf named after it (@slot f ≡ f = Slot{:f, Float64}()); the type argument defaults to Float64 and must be concrete (it is the type the term materializes into). The element type is computed at construction, so an ill-typed expression is rejected early.\n\nA bare Symbolic or Const is an AbstractScalar — not an AbstractTerm. The operators lift it into term-land via a Fill leaf the moment it interacts with a real term:\n\n@symbolic τ Float64                   # τ isa Symbolic <: AbstractScalar\nτ * f                                 # Term(*, (Fill(τ), f)) — Fill wraps τ\n@const α 2\nα + f                                 # Term(+, (Fill(α), f))\n2 * f                                 # Term(*, (Fill(Const(2)), f))\n\nSymbolics materialize to a single broadcast value (e.g. a timestep), unlike Slots which materialize to per-cell arrays.","title":"Building expressions"},{"category":"section","location":"guide/#Simplifying","page":"Guide","text":"simplify rewrites to a normal form — shifts pushed onto the leaves, nested shifts merged, identities collapsed, and all-Fill sub-expressions folded into one Fill(Scalar(…)) (the scalar-precedence rule: scalar arithmetic done once at compile time, not broadcast cell-by-cell):\n\n@slot g Float64\nsimplify(δ₊{1}(f + g))               # (f[ê₁] + g[ê₁]) - (f + g)\n\n# All-Fill collapse + scalar folding in one pass:\nsimplify(Fill(Const(2.0)) + Fill(Const(3.0)))   # Fill(Const(5.0))\nsimplify(2 * (f + 3*f))                         # (2*1 + 2*3 = 8) ⇒ collapses inside Fill\n\nThe identity rules detect a structural Zero/One by type, a Fill{<:Null}/Fill{<:Unity} by type (matching the scalar-side dispatch), and a literal Fill{<:Const} by value (iszero/isone on the wrapped literal). The last is a deliberate departure from a stricter no-auto-fold stance — f * Fill(Const(0.0)) annihilates to Zero{Float64}(), which is mathematically correct.","title":"Simplifying"},{"category":"section","location":"guide/#Differentiating-into-a-stencil","page":"Guide","text":"differentiate with respect to a slot yields a row-anchored Stencil whose per-offset coefficients are the partial derivatives:\n\ndifferentiate(δ₊{1}(f), f)           # offsets (ô, ê₁), coefficients (-1, 1)\n\n# variable coefficient — ∂(ψ·δ₊{1}(f))/∂f\n@slot ψ Float64\ndifferentiate(ψ * δ₊{1}(f), f)\n\n# nonlinear — ∂(f*f)/∂f = f + f\ndifferentiate(f * f, f)\n\nA Laplacian-shaped expression differentiates to the five-point star:\n\nlap = δ₋{1}(δ₊{1}(f)) + δ₋{2}(δ₊{2}(f))   # f[i±1] + f[j±1] - 4 f\ndifferentiate(lap, f)                     # a Stencil that narrows to a star\n\nDifferentiating with respect to a Symbolic collapses the per-offset structure to a single broadcast coefficient (an AbstractTerm, not a Stencil):\n\n@symbolic τ Float64\ndifferentiate(τ * f, τ)              # === f       (a term)\ndifferentiate(τ * f, f)              # a Stencil   (offset ô, coefficient Fill(τ))\n\nThe scalar pieces of the expression — anything inside a Fill{<:AbstractScalar} — are differentiated by StencilCore's scalar differentiate; the result is re-wrapped in a Fill on the way back into term-land.","title":"Differentiating into a stencil"},{"category":"section","location":"guide/#Building-and-assembling","page":"Guide","text":"build_stencil converts the row-anchored result to a column-anchored, assemblable stencil (narrowing to a LinearStencil/StarStencil and materializing the coefficients). Pass pad = true to fill single-axis offset gaps, and the mesh size for a constant coefficient. Then assemble with StencilAssembly:\n\nusing StencilCalculus, StencilAssembly\n\n@slot f Float64\nsst = differentiate(lap, f)\nst  = build_stencil(sst; size = (5, 4))     # → StarStencil{1, 2, 5, …}\nA   = build(st, (1:5, 1:4), (1:5, 1:4))     # SparseMatrixCSC\n\nFor a variable-coefficient operator, pass the substituted arrays instead of size:\n\nψv  = collect(1.0:8.0)\n@slot ψ Float64\nsst = differentiate(ψ * δ₊{1}(f), f)\nst  = build_stencil(sst, (ψ = ψv,))         # coefficients read from ψv","title":"Building and assembling"},{"category":"section","location":"guide/#Inspecting:-materialize-and-code_string","page":"Guide","text":"materialize compiles an expression into a read-only LazyArray; code_string renders the same per-cell kernel as source you can drop into a file:\n\nfv = rand(16); ψv = rand(16)\nla = materialize(adv, (f = fv, ψ = ψv))     # axes (1:15,); la[i] = ψv[i]*(fv[i+1]-fv[i])\n\nprint(code_string(adv; name = :advect))\n\nA Fill materializes its wrapped value (recursively, for an AbstractScalar) once per cell — so materialize(τ * f, (f = fv, τ = 0.5)) gives a kernel that reads args.τ * args.f[i], not args.τ[i] * args.f[i].","title":"Inspecting: materialize and code_string"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Terms,-operators,-and-operations","page":"API reference","text":"","title":"Terms, operators, and operations"},{"category":"section","location":"api/#Re-exported-from-StencilCore","page":"API reference","text":"The offset vocabulary, the AbstractTerm supertype, and the scalar algebra (AbstractScalar, Symbolic, Const, Null, Unity, Scalar, plus the @symbolic / @const macros) are re-exported from StencilCore (full docs there). The Stencil produced by differentiate and the narrowing functions live there too.","title":"Re-exported from StencilCore"},{"category":"type","location":"api/#StencilCalculus.Diff","page":"API reference","text":"Diff(v)   (alias ∂)\n\n\"With respect to\" functor: ∂(v)(e) == differentiate(e, v). v may be a Slot (the result is a spatial Stencil) or a Symbolic (the result is an AbstractTerm coefficient — a per-cell broadcast).\n\n\n\n\n\n","title":"StencilCalculus.Diff"},{"category":"type","location":"api/#StencilCalculus.Fill","page":"API reference","text":"Fill{T} <: AbstractTerm{T}\n\nBroadcast-to-grid bridge from scalar-land to term-land: wraps a single value (a literal or an AbstractScalar) and presents it as a spatially- invariant AbstractTerm. The element type follows the wrapped value: for a literal T it is T; for an AbstractScalar it is eltype(scalar) (recursive), so e.g. Fill(Symbolic{:τ, Float64}()) has eltype Float64 and arithmetic with a Slot{:f, Float64} promotes cleanly.\n\n\n\n\n\n","title":"StencilCalculus.Fill"},{"category":"type","location":"api/#StencilCalculus.FwdDiff","page":"API reference","text":"FwdDiff{D} / BwdDiff{D} / FwdSum{D} / BwdSum{D}   (aliases δ₊, δ₋, σ₊, σ₋)\n\nNon-local first-order operators along mesh axis D, applied by calling the type on a term:\n\noperator op{D}(t) meaning\nδ₊ FwdDiff t[i+1] - t[i] along D forward difference\nδ₋ BwdDiff t[i]   - t[i-1] backward difference\nσ₊ FwdSum t[i+1] + t[i] forward sum\nσ₋ BwdSum t[i]   + t[i-1] backward sum\n\nEach builds a Term over a Shifted leaf (e.g. δ₊{1}(f) == f[ê₁] - f); on a Number they collapse to the local closed form (0, 0, 2x, 2x). Dimension-polymorphic — only the axis D is fixed.\n\n\n\n\n\n","title":"StencilCalculus.FwdDiff"},{"category":"type","location":"api/#StencilCalculus.LazyArray","page":"API reference","text":"LazyArray{T, N, F, Args<:NamedTuple} <: AbstractArray{T, N}\n\nRead-only array view of a materialized term: fn(args, I...) computes the element at I, where fn is a generated per-cell kernel and args is the substitution NamedTuple. axes is the term's valid index box (each substituted array's axes shrunk inward by the term's shift footprint).\n\n\n\n\n\n","title":"StencilCalculus.LazyArray"},{"category":"type","location":"api/#StencilCalculus.Shifted","page":"API reference","text":"Shifted(shift::StaticShift, term) / Shifted(term, shift::StaticShift)\n\nA term read at the lattice offset shift. The element type is unchanged (eltype(term)); the zero shift ô is the identity (returns term).\n\n\n\n\n\n","title":"StencilCalculus.Shifted"},{"category":"type","location":"api/#StencilCalculus.Slot","page":"API reference","text":"Slot{S, T}()\n\nPlaceholder for a discrete field named S (a Symbol) whose cells hold values of the concrete type T (default Float64). Substituted with an AbstractArray at materialize and indexed per cell. Term-side analogue of Symbolic.\n\n\n\n\n\n","title":"StencilCalculus.Slot"},{"category":"type","location":"api/#StencilCalculus.Term","page":"API reference","text":"Term(fn, args::Tuple{Vararg{AbstractTerm}})\n\nInternal node applying fn to args component-wise. The element type T = Base.promote_op(fn, eltype.(args)...) is computed at construction; a Union{} result (e.g. genuine SVector inhomogeneity) is an unconstructable term and throws.\n\n\n\n\n\n","title":"StencilCalculus.Term"},{"category":"type","location":"api/#StencilCalculus.Zero","page":"API reference","text":"Zero{T}() / One{T}()\n\nType-level additive / multiplicative identities (structure, not data): they let differentiation collapse and simplify rewrite by dispatch. Lower to zero(T) / one(T). Scalar-side analogues: Null / Unity.\n\n\n\n\n\n","title":"StencilCalculus.Zero"},{"category":"function","location":"api/#StencilCalculus.build_stencil","page":"API reference","text":"build_stencil(sst::Stencil, pairs::NamedTuple = (;); size = nothing)\n\nLower a (typically differentiate-produced) Stencil to an assemblable LinearStencil / StarStencil{ColumnAccess} with a concrete coefficient:\n\nconvert RowAccess → ColumnAccess (shift each per-offset coefficient by −offset);\noptionally densify (pad = true) to fill single-axis offset gaps with Zero so a gappy result still narrows to LinearStencil;\nnarrow the offset pattern (as_linear / as_star), which switches the structure-of-arrays terms to the array-of-structs SVector coefficient;\nmaterialize that coefficient against pairs (variable coefficients) — for a constant coefficient pass the mesh size.\n\nThe result is ready for StencilAssembly.build / assemble.\n\n\n\n\n\n","title":"StencilCalculus.build_stencil"},{"category":"method","location":"api/#StencilCalculus.code_string-Tuple{AbstractTerm}","page":"API reference","text":"code_string(term::AbstractTerm; name = :kernel, ndims = <hint>) -> String\n\nRender the per-cell kernel for term as Julia source — the same Expr materialize compiles, wrapped in a named function definition. ndims defaults to the largest shifted axis (≥ 1). For inspection / dumping to a file.\n\n\n\n\n\n","title":"StencilCalculus.code_string"},{"category":"method","location":"api/#StencilCalculus.densify-Tuple{Stencil}","page":"API reference","text":"densify(sst::Stencil) -> Stencil\n\nPad a single-axis Stencil to a contiguous offset range by inserting type-level Zero coefficients at the missing offsets, so a gappy differentiation result (e.g. offsets {-2, 0}) can narrow to LinearStencil. A no-op (returns sst) for patterns that are already contiguous, multi-axis, or pure-diagonal (ambiguous axis) — those go to as_star or stay as is.\n\n\n\n\n\n","title":"StencilCalculus.densify"},{"category":"method","location":"api/#StencilCore.differentiate-Union{Tuple{S}, Tuple{AbstractTerm, Slot{S}}} where S","page":"API reference","text":"differentiate(t::AbstractTerm, ::Slot{S}) -> StencilCore.Stencil{RowAccess}\n\nDifferentiate t with respect to the field named S (matched on the symbol only). Returns a row-anchored Stencil in structure-of-arrays form: reverse- lex-ordered offsets shifts and a parallel tuple terms whose k-th entry is the coefficient ∂t/∂(f[σ_k]). Throws if t does not depend on S (identically-zero derivative).\n\n\n\n\n\n","title":"StencilCore.differentiate"},{"category":"method","location":"api/#StencilCore.differentiate-Union{Tuple{S}, Tuple{AbstractTerm, Symbolic{S}}} where S","page":"API reference","text":"differentiate(t::AbstractTerm, ::Symbolic{S}) -> AbstractTerm\n\nDifferentiate t with respect to the named scalar parameter S. A Symbolic appears in t only inside a Fill, so the derivative is a per-cell broadcast coefficient (a single AbstractTerm, not a Stencil). Throws if t does not depend on S.\n\n\n\n\n\n","title":"StencilCore.differentiate"},{"category":"function","location":"api/#StencilCore.materialize","page":"API reference","text":"materialize(term::AbstractTerm, pairs::NamedTuple) -> LazyArray\n\nSubstitute the slots/symbolics named in pairs into term and lower it to a compiled LazyArray. The grid rank N is the substituted arrays' ndims (which must agree); the result's axes are their axes intersected and shrunk inward by the term's shift footprint (no implicit broadcasting). Shifts become index arithmetic in the generated kernel. Method on the same generic as StencilCore.materialize for AbstractScalar.\n\n\n\n\n\n","title":"StencilCore.materialize"},{"category":"function","location":"api/#StencilCore.simplify","page":"API reference","text":"simplify(t::AbstractTerm, rules = DEFAULT_RULES; maxsteps = 64)\n\nRewrite t to a normal form by post-walking and applying rules to a fixed point. The default rules push shifts down to leaves (merging nested shifts), apply additive/multiplicative identities, and collapse all-Fill Terms into a single Fill(Scalar(…)) (the scalar-precedence rule). Method on the same generic as StencilCore.simplify for AbstractScalar.\n\n\n\n\n\n","title":"StencilCore.simplify"},{"category":"macro","location":"api/#StencilCalculus.@slot","page":"API reference","text":"@slot name [T = Float64]\n\nBind name to Slot{:name, T}(), taking the variable name as the field symbol. @slot f ≡ f = Slot{:f, Float64}(); @slot f Float32 ≡ f = Slot{:f, Float32}(). Scalar-side analogue: @symbolic.\n\n\n\n\n\n","title":"StencilCalculus.@slot"},{"category":"type","location":"api/#StencilCore.AbstractTerm","page":"API reference","text":"AbstractTerm{T}\n\nSupertype of every symbolic grid expression. An AbstractTerm{T} behaves like a dimension- and size-less array whose eltype is T: its grid rank N is unknown until it is materialized against concrete arrays, but its element type T (the value each cell will hold once materialized) is fixed at construction.\n\neltype(::AbstractTerm{T}) === T. Concrete subtypes are provided by the StencilCalculus package.\n\n\n\n\n\n","title":"StencilCore.AbstractTerm"},{"category":"type","location":"api/#StencilCore.AbstractScalar","page":"API reference","text":"AbstractScalar{T}\n\nSupertype for cell-level scalar expressions. Reaches a single value of type T at materialize time (no axes). Concrete subtypes: Symbolic, Const, Scalar, Null, Unity. Sibling of, not subtype of, AbstractTerm.\n\n\n\n\n\n","title":"StencilCore.AbstractScalar"},{"category":"type","location":"api/#StencilCore.Symbolic","page":"API reference","text":"Symbolic{S, T}()\n\nNamed, runtime-substituted scalar parameter S (a Symbol) of concrete type T (default Float64). Materializes to the value supplied at the keyword S of the pairs NamedTuple — like Slot but without per-cell indexing.\n\n\n\n\n\n","title":"StencilCore.Symbolic"},{"category":"type","location":"api/#StencilCore.Const","page":"API reference","text":"Const(value) / Const{T}(value)\n\nLiteral scalar leaf carrying its value in a runtime field. The element type is typeof(value) (always concrete).\n\n\n\n\n\n","title":"StencilCore.Const"},{"category":"type","location":"api/#StencilCore.Null","page":"API reference","text":"Null{T}()\n\nType-level additive identity / structural zero for AbstractScalar: the scalar-side analogue of AbstractTerm Zero. Materializes to zero(T); lets the scalar simplify and differentiate rules collapse by dispatch.\n\n\n\n\n\n","title":"StencilCore.Null"},{"category":"type","location":"api/#StencilCore.Unity","page":"API reference","text":"Unity{T}()\n\nType-level multiplicative identity for AbstractScalar: the scalar-side analogue of AbstractTerm One. Materializes to one(T).\n\n\n\n\n\n","title":"StencilCore.Unity"},{"category":"type","location":"api/#StencilCore.Scalar","page":"API reference","text":"Scalar(fn, args::Tuple{Vararg{AbstractScalar}})\n\nInterior node of a scalar-tree: applies fn to scalar args component-wise. The element type T = Base.promote_op(fn, eltype.(args)...) is computed at construction; a Union{} result throws (the node is unconstructable). Term-side analogue: AbstractTerm Term.\n\n\n\n\n\n","title":"StencilCore.Scalar"},{"category":"type","location":"api/#StencilCore.StaticPair","page":"API reference","text":"StaticPair{D, O}\n\nA single offset of magnitude O along mesh dimension D (both compile-time Ints). Singleton. Alias: SPair.\n\n\n\n\n\n","title":"StencilCore.StaticPair"},{"category":"type","location":"api/#StencilCore.SPair","page":"API reference","text":"SPair\n\nAlias for StaticPair.\n\n\n\n\n\n","title":"StencilCore.SPair"},{"category":"type","location":"api/#StencilCore.StaticShift","page":"API reference","text":"StaticShift{P<:Tuple{Vararg{StaticPair}}}\n\nA lattice offset: a normalized collection of StaticPairs. Invariants (enforced by the constructor): pairs are sorted ascending by dimension, no two pairs share a dimension (same-dimension pairs are summed), and no pair has zero offset (dropped). The empty shift StaticShift{Tuple{}} is the identity.\n\nConstruct from pairs, or via the basis symbols ê₁ … ê₉ and the +/-/*Int algebra:\n\n3ê₁ + ê₂            # StaticShift{Tuple{StaticPair{1,3}, StaticPair{2,1}}}\n\nAlias: SShift.\n\n\n\n\n\n","title":"StencilCore.StaticShift"},{"category":"type","location":"api/#StencilCore.SShift","page":"API reference","text":"SShift\n\nAlias for StaticShift.\n\n\n\n\n\n","title":"StencilCore.SShift"},{"category":"constant","location":"api/#StencilCore.ô","page":"API reference","text":"ô, ê₁ … ê₉\n\nPredefined StaticShift constants: ô is the zero shift (identity), and êᵢ is the unit offset along axis i. Combine them with the +/-/*Int algebra to write lattice offsets, e.g. -2ê₁, 3ê₁ + ê₂.\n\n\n\n\n\n","title":"StencilCore.ô"},{"category":"type","location":"api/#StencilCore.Stencil","page":"API reference","text":"Stencil{M, C<:NTuple{M,StaticShift}, A<:NTuple{M,ArrayOrTermLike}, S<:AccessStyle}\n    <: AbstractStencil{S}\n\nGeneral offset-list stencil in structure-of-arrays form: M offsets shifts (reverse-lexicographically ordered) and a parallel M-tuple terms of per-offset coefficients (terms[k] is the coefficient at offset shifts[k]). Each coefficient is an ArrayOrTermLike — a concrete array or a symbolic term — with a scalar element type.\n\nThe form produced by symbolic differentiation; not assembled directly — narrowed to a LinearStencil / StarStencil via as_linear / as_star, which is where the layout switches to array-of-structs (one SVector{M}-valued coefficient) by _interlaceing terms.\n\n\n\n\n\n","title":"StencilCore.Stencil"},{"category":"function","location":"api/#StencilCore.as_linear","page":"API reference","text":"as_linear(st::Stencil{…,S}) -> LinearStencil{D, …, S}\n\nNarrow a Stencil whose offsets are single-axis (same axis D) and contiguous to the equivalent LinearStencil{D}, interlacing terms into the single SVector{D}-valued coefficient. Throws if the offsets are multi-axis, span several axes, or are not contiguous-ascending.\n\n\n\n\n\n","title":"StencilCore.as_linear"},{"category":"function","location":"api/#StencilCore.as_star","page":"API reference","text":"as_star(st::Stencil{…,S}) -> StarStencil{L, …, S}\n\nNarrow a Stencil whose offsets form the canonical reverse-lex star pattern (every axis 1..N with symmetric reach −L..L, plus the diagonal) to the equivalent StarStencil{L}, interlacing terms into the single SVector{M}- valued coefficient. Throws otherwise.\n\n\n\n\n\n","title":"StencilCore.as_star"},{"category":"section","location":"differentiation/#Differentiation,-display,-and-the-algebra-behind-it","page":"Differentiation","text":"This page collects the design decisions around differentiating grid expressions and displaying them, and then steps back to look at the mathematical structure they sit in. It is part design record, part roadmap: each section flags clearly what the package does today versus what is a direction not yet built.","title":"Differentiation, display, and the algebra behind it"},{"category":"section","location":"differentiation/#Two-parallel-algebras","page":"Differentiation","text":"A grid expression has two leaf kinds, with sibling algebras that mirror each other (see also StencilCore):\n\nrole AbstractScalar (Core) AbstractTerm (here)\nnamed substitution leaf Symbolic{S, T} Slot{S, T}\nliteral carrier Const{T} with val::T — (via Fill(Const(…)))\ninterior tree node Scalar{F, A, T} Term{F, A, T}\nadditive identity Null{T} Zero{T}\nmultiplicative identity Unity{T} One{T}\n\nA scalar has no spatial extent (one materialized value); a term is array-like (per-cell). The two are bridged by Fill{T}, a term that wraps a single value (literal or AbstractScalar) and broadcasts it spatially. Term.args is always Tuple{Vararg{AbstractTerm}} — scalars never appear inside a Term directly; they enter through a Fill leaf at the operator boundary.","title":"Two parallel algebras"},{"category":"section","location":"differentiation/#Scalars-are-shift-invariant","page":"Differentiation","text":"A bare AbstractScalar — a Symbolic, a Const, a Null, a Unity, or an interior Scalar tree — has no spatial extent. Translating it is therefore the identity:\n\n@symbolic τ Float64\nτ[ê₁]              # === τ\nτ[3ê₁ + ê₂]        # === τ\nConst(2.0)[ê₁]     # === Const(2.0)\n\nA Fill in term-land carries the same property: it is the spatially-invariant broadcast of one value, so indexing it is also a no-op:\n\nFill(τ)[ê₁]        # === Fill(τ)\nFill(Const(2.0))[3ê₁ + ê₂]   # === Fill(Const(2.0))\n\nThe same rule lives in simplify as rule_shift_const: Shifted(s, ::Fill) → ::Fill, and analogously for Zero and One. A shift can never reach a position-independent leaf, whether it is written directly (f[ê₁] collapses) or produced by pushing a shift down a tree.","title":"Scalars are shift-invariant"},{"category":"section","location":"differentiation/#Displaying-the-normal-form","page":"Differentiation","text":"show renders a term in component form, and it renders the term's normal form — it calls simplify first (display only; the object is never mutated). The conventions:\n\nterm shows as\nSlot{:f}() f[]\nf[ê₁] (a shift) f[ê₁]\nZero{T}() 0\nOne{T}() 1\nFill(Const(2.0)) 2.0\nFill(Symbolic{:τ,T}()) τ\nFill(Scalar(*, (Const(2), τ))) (2 * τ)\nTerm infix for + - * / \\ ^, else call form\n\n@slot f\n@symbolic τ\nrepr(τ * δ₊{1}(f))           # \"(τ * (f[ê₁] - f[]))\"\nrepr(f - Zero{Float64}())    # \"f[]\"  — display is of the simplified form\n\nA Fill prints as its wrapped value via the scalar-side show, without square brackets — Fill(τ) is τ, not τ[]. The bracket notation is reserved for spatially-indexed leaves (Slot and Shifted).\n\nnote: Why the glyphs `0`/`1`\nZero/One (and their scalar-side analogues Null/Unity) are symbolic identities — structure, not data — so they print as the bare glyphs 0/1 regardless of T. Zero{Float64} shows 0, not 0.0. The display stays type-agnostic and no value is ever constructed, which keeps it faithful to their role as the structural neutrals that drive simplify and make the chain rule collapse.","title":"Displaying the normal form"},{"category":"section","location":"differentiation/#Differentiation:-the-concrete-behaviour","page":"Differentiation","text":"differentiate (and its sugar ∂, below) walks the expression with a ChainRules-style frule table (derivative) and the chain rule. There are two methods on the same generic, dispatched by the kind of the variable:\n\ndifferentiate(t::AbstractTerm, ::Slot) walks the term tree and collects one coefficient per lattice offset — a row-anchored Stencil.\ndifferentiate(t::AbstractTerm, ::Symbolic) walks the term tree but defers Fill{<:AbstractScalar} leaves to the scalar-side differentiate (a third method, on the same generic, for AbstractScalar → AbstractScalar). The result is re-wrapped in a Fill on the way back into term-land, producing a single broadcast coefficient — an AbstractTerm, not a Stencil.\n\nIs the derivative of One{T}() equal to Zero{T}()? Effectively yes, but no literal Zero object is produced for a Slot-independent leaf. One, Zero, Fill and a Shifted of an unrelated Slot all contribute the empty set of offset/coefficient pairs to the Slot derivative — which is exactly \"zero\" in this representation. A derivative that is empty everywhere makes differentiate throw rather than return a degenerate stencil: the package deliberately does not fabricate spurious zeros.\n\nOn the scalar side the same story plays out with Null instead of an empty set: a Const, Null, Unity, or a Symbolic that does not match the differentiation variable returns Null{T}(). The chain-rule walker short-circuits Null sub-derivatives, so an unsupported primitive does not error when its branch contributes nothing.","title":"Differentiation: the concrete behaviour"},{"category":"section","location":"differentiation/#—-the-\"with-respect-to\"-functor","page":"Differentiation","text":"∂ (alias Diff) wraps the variable and lowers to differentiate:\n\n∂(v)(e) == differentiate(e, v)\n\nThe variable's kind decides the result type:\n\n∂(slot) → a Stencil. A slot is a spatially-extended field, so the derivative carries offsets — it is a row-anchored stencil.\n∂(symbolic) → an AbstractTerm. A symbolic has no spatial extent, so the per-offset structure collapses to a single coefficient term:\n\n@symbolic τ Float64\n@slot f Float64\n∂(τ)(τ * f)        # === f          (a term)\n∂(f)(τ * f)        # a Stencil      (offset ô, coefficient Fill(τ))\n\nA Slot and a Symbolic that happen to share a symbol do not collide: the differentiation variable is matched by instance type, not just by its name, so ∂(Symbolic{:τ}()) and ∂(Slot{:τ}()) differentiate against different leaves.","title":"∂ — the \"with respect to\" functor"},{"category":"section","location":"differentiation/#The-scalar-precedence-rule","page":"Differentiation","text":"A Term whose arguments are all Fill is the broadcast of a scalar expression — there is no spatial structure for the operation to act on cell-by-cell. simplify therefore collapses it into a single Fill(Scalar(fn, vals…)), with the inner scalar then simplified by StencilCore's scalar simplify:\n\nsimplify(Fill(Const(2.0)) + Fill(Const(3.0)))            # Fill(Const(5.0))\nsimplify(Fill(Const(2.0)) * Fill(Symbolic{:τ,Float64}())) ==\n    Fill(Scalar(*, (Const(2.0), Symbolic{:τ,Float64}())))\n\nThe rule only fires when every argument is a Fill — a Term(*, (Fill(τ), f)) (with a real Slot f) stays a Term, because there is genuine spatial structure to broadcast over.\n\nThe corresponding identity check (f * Fill(Const(0)) → Zero) uses a small helper, _is_term_zero / _is_term_one, that is type-dispatched on Zero/One and on Fill{<:Null} / Fill{<:Unity}, and value-dispatched on a literal Fill{<:Const} (via iszero/isone on the wrapped value). A Fill wrapping a symbolic scalar (Fill(Symbolic{S}), Fill(Scalar(…))) is never treated as an identity — its runtime value is unknown.","title":"The scalar-precedence rule"},{"category":"section","location":"differentiation/#Beyond-Number:-tensor-valued-fields-(direction)","page":"Differentiation","text":"AbstractTerm{T} and AbstractScalar{T} already carry the materialized element type T. Nothing in the structure of differentiation assumes T <: Number; only the derivative rules do. If a slot is SVector-valued and we differentiate with respect to an SVector-valued variable, the natural coefficient is an SMatrix — the local Jacobian block, by the usual convention\n\nJ[i, j] = ∂(output_i) / ∂(input_j)         # rows: output, columns: input\n\nso Number × Number → Number, SVector{m} × SVector{n} → SMatrix{m, n}. Supporting this fully would require three things, none of them yet built:\n\nMatrix-valued derivative rules for the vector primitives (e.g. ∂(A * x)/∂x = A, ∂‖x‖/∂x = xᵀ/‖x‖).\nA non-commutative chain rule. With scalar coefficients the order of the chain-rule product is irrelevant; with matrix blocks it is not — the product must be assembled outer-to-inner in the correct order.\nAn element-type promotion _jac_eltype(T_out, T_in) driving the coefficient's type, replacing the scalar * currently used to combine partials.\n\nThe point worth recording: the type-as-structure design already reserves the slot for this (the element type is tracked); it is the rule table and the combiner that would grow.","title":"Beyond Number: tensor-valued fields (direction)"},{"category":"section","location":"differentiation/#The-hidden-duality:-variance","page":"Differentiation","text":"There is a tensor-calculus structure under all of this. A Jacobian ∂Fⁱ / ∂xⱼ has one contravariant index (the output i, from F) and one covariant index (the input j, from the variable x) — it is a mixed (1,1)-tensor, i.e. a linear map V → W ≅ W ⊗ V*. This is not decoration; it maps directly onto two existing design choices.\n\nAccessStyle is which index is anchored. A stencil's coefficient is indexed by a single mesh point plus an offset; the anchor says whether that point is the input or the output. ColumnAccess (CSC) anchors the input (covariant) index — the column is the variable we differentiate against; RowAccess (CSR-to-be) anchors the output (contravariant) index — the row is the equation. differentiate emits RowAccess because the row is where the derivative is naturally evaluated.\nCSC ↔ CSR is the transpose. The bridge converts a row-anchored result to a column-anchored one by shifting each coefficient by -σ. That sign flip is exactly the index swap of a transpose / adjoint: a shift acting covariantly on the input becomes a shift on the output. The implicit Jacobian ∂F/∂ψ and the explicit −∂F/∂φ are maps out of two different tangent spaces; their adjoints live in the dual.\n\nThe practical upshot is that \"offsets are invariant under AccessStyle, only the anchoring shifts\" (a StencilCore rule) is the discrete shadow of raising and lowering one index of a (1,1)-tensor.","title":"The hidden duality: variance"},{"category":"section","location":"differentiation/#Stencils-as-morphisms:-application,-algebra,-composition","page":"Differentiation","text":"An AbstractStencil is a transformation of fields: it should take a term and return a term. That operation is not implemented today, but it is the right way to think about the type, and it closes a loop with differentiation.","title":"Stencils as morphisms: application, algebra, composition"},{"category":"section","location":"differentiation/#Applying-a-stencil-to-a-term-(direction)","page":"Differentiation","text":"The natural definition is\n\nS(t) = Σ_k  coef_k · t[σ_k]\n\n— sum the coefficient at each offset times the field read at that offset. With this, for an F that is linear and homogeneous in f, differentiating and then re-applying recovers the original:\n\ndifferentiate(F, f)  applied back to  f   ==   F        (F linear in f)\n\nThis is not the identity for a general F: constants and other slots are annihilated by ∂/∂f, so only the part of F that is linear and homogeneous in f comes back. Stating it precisely is the useful part — it is the discrete \"a linear map equals its own Jacobian.\"","title":"Applying a stencil to a term (direction)"},{"category":"section","location":"differentiation/#A-monoid-and-a-ring,-not-a-group","page":"Differentiation","text":"The intuition that applying a stencil is a kind of multiplication is correct, and composing two stencils is composition of linear operators. But the structure is a monoid under composition (associative; the identity is the single-offset ô stencil with coefficient 1) and, together with addition, a unital associative algebra — not a group. A finite-difference stencil has no stencil inverse (the inverse of a differential operator is a dense integral operator), so inverses are simply absent.\n\nConcretely:\n\nConstant coefficients form a commutative ring — the Laurent polynomials in the shift operators, ℝ[ê₁^±¹, …, ê_N^±¹]. Composition is convolution of offset patterns; everything commutes.\nVariable coefficients form a non-commutative ring — a difference-operator (Ore/skew) algebra, because a shift does not commute with multiplication by a position-dependent coefficient:\nê ∘ (a · )  =  (Sₐ · ) ∘ ê        where  Sₐ  is a shifted by ê\nThis commutation law is precisely what a correct composition routine has to honour.","title":"A monoid and a ring, not a group"},{"category":"section","location":"differentiation/#Composing-stencils-(direction)","page":"Differentiation","text":"Composition S₁ * S₂ is a modest extension of what already exists: the StaticShift + algebra already composes offsets, so a composition routine walks pairs of offsets, adds them, and multiplies the coefficients — applying the shift commutation above to the inner coefficient. It is deferred, not difficult.","title":"Composing stencils (direction)"},{"category":"section","location":"differentiation/#A-category-theory-reading","page":"Differentiation","text":"The cleanest frame for everything above is categorical.\n\nObjects are field spaces over the mesh (an element type plus an index domain). Morphisms are linear stencils. Composition is associative and every object has an identity stencil, so this is a category 𝒮 — a subcategory of R-modules. Hom-sets are themselves vector spaces (stencils add), so 𝒮 is enriched in Vect; the endomorphisms of one object are the operator algebra of the previous section.\nDifferentiation is a differential combinator. The pairing of a derivative table with the chain rule is exactly the structure axiomatised by Cartesian differential categories (Blute–Cockett–Seely) and tangent categories (Cockett–Cruttwell): an operator D[-] sending a map to its linearization, with the chain rule as its coherence law. What the package implements today is this combinator on the Slot/Symbolic fragment.\nAccessStyle is the dagger. CSC versus CSR — a morphism versus its transpose — is the adjoint, i.e. the † : 𝒮 → 𝒮ᵒᵖ structure. The bridge's -σ shift is the concrete formula for it.\n\"Jacobian, you said?\" resolved. On a mesh of dimension > 1 these operators are not literally Jacobian matrices until the unknowns are given a linear numbering. That numbering is a faithful functor 𝒮 → FinVect_ℝ (LinearIndices flattening each field space to ℝⁿ), under which a stencil becomes its honest SparseMatrixCSC. The abuse of calling the stencil a \"Jacobian\" is the informal name for its image under this functor.\n\nWhat is implemented: the linear category of stencils, the parallel scalar algebra, and the differential combinator on slots and symbolics. What is aspirational: stencil application and composition (*), the dagger as an explicit operation, and the tensor-valued (SMatrix) blocks of the section above.","title":"A category-theory reading"},{"category":"section","location":"#StencilCalculus.jl","page":"Home","text":"StencilCalculus is a small Computer Algebra System for expressions on N-D Cartesian grids, with one purpose: build sparse stencils by differentiating them. It is the symbolic front of a small stack — it depends on StencilCore for the term and stencil types (and for the parallel scalar algebra), and hands its results to StencilAssembly for assembly into a SparseMatrixCSC.","title":"StencilCalculus.jl"},{"category":"section","location":"#Why-differentiate-a-grid-expression?","page":"Home","text":"On a structured mesh, the operations of interest often define a field ψ implicitly as the root of a (possibly nonlinear) system\n\nF(psi phi) = 0\n\nfor instance a compact finite-difference scheme. Solving it with a Newton-type method needs the Jacobians of F: the implicit Jacobian ∂F/∂ψ and the explicit Jacobian −∂F/∂φ. Because the same formula is applied at every node, these Jacobians are sparse with a repeating pattern — a stencil.\n\nWriting those stencils by hand is tedious and error-prone, especially with variable coefficients. Instead, you write F as a symbolic grid expression and let the package differentiate it into a stencil.\n\nnote: “Jacobian”, you said?\nStrictly, on a mesh of dimension greater than one these objects are not Jacobians until the unknowns are flattened to a linear numbering — which is exactly what assembling into a SparseMatrixCSC does. We use the word loosely; the package makes the flattening precise.","title":"Why differentiate a grid expression?"},{"category":"section","location":"#Two-algebras,-one-bridge","page":"Home","text":"A grid expression has two kinds of leaves:\n\nSpatially-extended fields (Slot) — substituted with an array at materialize, indexed per cell.\nCell-level scalars (AbstractScalar: Symbolic, Const, Null, Unity, and the interior Scalar tree node) — defined in StencilCore, materialize to a single value, never carry a spatial index.\n\nThese are sibling algebras: a Term.args tuple is always Tuple{Vararg{AbstractTerm}}, never mixed with scalars. Scalars cross into term-land via Fill — a Fill{T} <: AbstractTerm{T} is the spatially-invariant broadcast of one value (literal or AbstractScalar). eltype(Fill{<:AbstractScalar}) unwraps recursively so e.g. Slot{:f,Float64}() + Fill(τ) promotes to Float64. Operators on the boundary do the lifting for you: 2 * f, τ * f, α + f all build Terms with Fill(…) leaves.","title":"Two algebras, one bridge"},{"category":"section","location":"#How-it-fits-together","page":"Home","text":"The data structures follow one rule, inherited from StencilCore — type parameters are structure; values are data: lattice offsets live at the type level, coefficients are ordinary (or lazy) arrays.\n\nBuild an expression from Slots (discrete fields), Symbolics and Consts (scalar parameters and literals, from StencilCore), pointwise operators, and the non-local difference/sum functors δ₊/δ₋/σ₊/σ₋. Index sugar f[-ê₁] shifts a field.\nsimplify the expression to normal form (shifts pushed onto the leaves; identities collapsed; all-Fill sub-expressions collapsed via the scalar-precedence rule into a single Fill(Scalar(…))).\ndifferentiate with respect to a Slot → a row-anchored Stencil; or with respect to a Symbolic → an AbstractTerm (per-cell broadcast coefficient).\nbuild_stencil converts the slot derivative to a column-anchored, assemblable LinearStencil/StarStencil, materializing the coefficients.\nAssemble the result with StencilAssembly.\n\nFor inspection, materialize compiles an expression into a read-only lazy array, and code_string renders the same per-cell kernel as Julia source.","title":"How it fits together"},{"category":"section","location":"#Quickstart","page":"Home","text":"using StencilCalculus, StencilAssembly\nusing StaticArrays\n\n@slot f Float64\n@slot ψ Float64\n\n# Upwind advection:  out[i] = ψ[i] * (f[i+1] - f[i])\nexpr = ψ * δ₊{1}(f)\n\nprint(code_string(expr; name = :advect))\n#   function advect(args, i)\n#       args.ψ[i] * (args.f[i + 1] - args.f[i])\n#   end\n\n# Differentiate w.r.t. f, then build and assemble the Jacobian.\nsst = differentiate(expr, f)              # a row-anchored Stencil\nst  = build_stencil(sst, (ψ = rand(16),)) # → an assemblable LinearStencil\nA   = build(st, (1:15,), (1:15,))         # SparseMatrixCSC\n\nSee the Guide for the Laplacian and the full end-to-end loop, and the API reference.","title":"Quickstart"}]
}
